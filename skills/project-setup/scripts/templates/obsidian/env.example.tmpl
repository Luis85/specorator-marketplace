# Copy to .env.local (gitignored). Dev builds (`npm run dev`) copy main.js,
# manifest.json, and styles.css into this vault's plugin folder and write a
# .hotreload marker for the community "Hot Reload" plugin.
OBSIDIAN_VAULT=/absolute/path/to/your/test-vault
