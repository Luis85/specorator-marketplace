# Obsidian's community-plugin updater matches a release by tag === manifest
# version, so `npm version` must tag WITHOUT npm's default "v" prefix
# (v0.1.1 would never match version 0.1.1).
tag-version-prefix=""
