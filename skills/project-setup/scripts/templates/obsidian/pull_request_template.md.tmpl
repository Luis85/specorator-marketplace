## What

<!-- One paragraph: what changes and why. Link the issue/ADR if one exists. -->

## Changes

-

## Verification

- [ ] `lint` / `typecheck` / `test` pass locally
- [ ] Ratchets green (`check:loc`, `check:css`, `check:quality` with `coverage/` absent)
- [ ] `build` + `check:artifacts` pass
- [ ] Ratchet improvements locked in via `--update` (baseline diff committed)
- [ ] Exercised in a real vault (`npm run dev` + hot reload) for UI changes

## Screenshots

<!-- For UI changes: before/after, light + dark theme. Delete if not applicable. -->
